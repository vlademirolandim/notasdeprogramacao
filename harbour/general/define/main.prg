/*
Compile com hbmk2 main (veja o hbp em anexo)
*/
PROCEDURE MAIN

    CLS
    ? "VALUES DEFINED IN main.hbp (COMPILATION TIME)"
    ? INCLUI
    ? HOJE
    ? VERSION + 100
    ? DATE_VERSION	 

RETURN
