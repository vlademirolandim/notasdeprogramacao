/*
Returns name and directory of the current Harbour program

*/
PROCEDURE MAIN

     ? "Returns name and directory of the current Harbour program", HB_ProgName() 
     ? "Equivalent ", ExeName()

     RETURN
